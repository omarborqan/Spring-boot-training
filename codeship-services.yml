hotel:
  build:
    image: omarborqan/spring-boot-training
    dockerfile: Dockerfile.build
    volumes:
      - ./data:/target

deploy:
  image: dind
  dockerfile: Dockerfile.deploy
  volumes_from:
    - hotel

