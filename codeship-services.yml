networks:
  web:
hotel:
  build: 
    image: omarborqan/Spring-boot-training
    dockerfile: ./Dockerfile
  depends_on:
    - mysql
    - keycloak
  networks:
    - web

mysql:
  image: mysql:5.7
keycloak:
  image: quay.io/keycloak/keycloak
